final class AppConstants {
  AppConstants._();

  static const appName = 'Majadigi Mobile';
  static const defaultConnectTimeout = Duration(seconds: 15);
  static const defaultReceiveTimeout = Duration(seconds: 15);
}
